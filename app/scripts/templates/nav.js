import $ from 'jquery';
import Backbone from 'backbone';

let nav = $(`
		<h1>Pub Order</h1>
		<nav>
			<a href="#beer">Beer</a>
			<a href="#entrees">Entrees</a>
			<a href="#games">Games</a>
		</nav>
  `);

  export default nav;
